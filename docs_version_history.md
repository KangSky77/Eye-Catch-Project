# Eye-Catch 버전 계보 (2026-04 ~ 2026-08)

> 이 프로젝트는 도중에 여러 갈래로 갈라졌다가 다시 하나로 합쳐졌다.
> USB로 옮겨 다니며 작업한 탓에 폴더가 다섯 개 남아 있어서, **어느 폴더가 무엇이고
> 무엇이 살아남았는지**를 한 번 정리해 둔다. 지금 살아 있는 저장소는 이 폴더
> (`C:\eye_catch`) 하나뿐이고, 나머지 넷은 참고용 스냅샷이다.

## 폴더 다섯 개

| 폴더 | 마지막 작업 | 백엔드 | 프론트 JS | 상태 |
|---|---|---|---|---|
| `eye_catch_old` | 2026-06-09 | 200줄 | 399줄 | 스냅샷 (미완성) |
| `eye_catch_codex` | 2026-06-12 | 404줄 (단일 `main.py`) | 659줄 | 스냅샷 |
| `eye_catch_Antigravity` | 2026-06-17 | 575줄 | 977줄 | 스냅샷 |
| `eye_catch_claude` | 2026-06-23 | 1,151줄 | 1,153줄 | 스냅샷 (종강 시점) |
| **`eye_catch`** | **2026-08-26** | **1,605줄** | **3,167줄** | **현행** |

세 갈래(codex / Antigravity / claude)는 같은 시기에 병렬로 굴린 버전이고,
6월 말에 좋은 부분만 골라 하나로 합친 것이 지금의 `eye_catch`다.

---

## 1. 각 스냅샷이 남긴 것

### `eye_catch_old` — 골격만 있던 시절

프론트는 이미 4개 엔드포인트(`/api/analyze-eye`, `/api/get-ai-opinion`,
`/api/chat-with-gemma`, `/api/generate-next-question`)를 부르는데 백엔드
`routes.py`에는 `POST /analyze` 하나뿐이다. `llm.py`에 함수는 다 있지만 라우터에
연결이 안 돼 있어서, **이 폴더는 그대로 띄우면 동작하지 않는다.** 리팩터링 중간에
멈춘 스냅샷으로 보는 게 맞다.

추론도 `argmax`만 써서 `"Cataract"` / `"Normal"` 문자열만 돌려준다 — softmax 확률이
없으니 지금과 같은 확률 표시·경계 판정이 구조적으로 불가능했다.

### `eye_catch_codex` — 눈 크롭과 운영 설정

단일 `main.py` 404줄. 여기서 나온 것 중 지금까지 살아남은 것:

- **MTCNN 랜드마크로 두 눈 영역을 잘라 분석** — 현행 `eye_detector.py`의 조상
- **`WeightedRandomSampler`** (배치 내 클래스 비율 50:50) + **`balanced_accuracy + 0.25·F1`
  모델 선택 기준** — 현행 학습 스크립트가 그대로 쓰고 있다
- **`ALLOWED_ORIGINS` CORS 화이트리스트** — 오래 잊혔다가 2026-08-26에 되가져왔다 (아래 5장)

살아남지 않은 것: `mobilenet_v3_small` 백본(→ EfficientNet-B0), `remove_glare()`
(→ 반사 게이트로 대체, 이유는 5장).

### `eye_catch_Antigravity` — RAG와 양안 판정의 초안

- **`medical_guidelines.json`** — 의학 지식을 코드 밖 JSON으로 분리한 첫 시도.
  이 아이디어는 2026-08-26에 `data/medical_knowledge.json`으로 되살렸다 (5장)
- `rag.py` — TF-IDF + 한국어 조사 stemming 검색. 현행은 언어 중립 코드로 KB를
  직접 조회하는 방식이라 이 코드 자체는 쓰지 않는다. 6개국어를 지원하는 상황에서
  텍스트 유사도 검색은 언어마다 품질이 달라지는 반면, 코드 조회는 언어와 무관하다
- 양안 판정 초안 — 가로가 세로의 1.3배 이상이면 좌우로 반 잘라 각각 분석하는
  종횡비 휴리스틱. 현행은 MTCNN 기반이라 사진 구도에 의존하지 않는다
- 병원 목록은 하드코딩 목업이었다 (`clinic1 / 02-123-4567`)

또 하나 기록해 둘 것: 학습 시 `WeightedRandomSampler`와 손실 가중치를 **둘 다**
주면 이중 보정이 된다는 지적이 이 갈래에서 나왔고, 그걸 반영해 손실 함수를 가중치
없는 `CrossEntropyLoss`로 바꿨다. FP가 12 → 11로 오히려 줄었다.

### `eye_catch_claude` — 종강 시점 (2026-06-23)

세 갈래를 합치고, **이 프로젝트에서 가장 중요한 발견**을 한 지점이다.

`dedup_dataset.py`로 전수 검사한 결과 **원본 16,816장 중 8,639장(약 51%)이 다른
사진의 근접중복**이었다. 사진 단위로 무작위 분할하던 그때까지의 방식으로는 같은
사진이 train과 test에 동시에 들어갔고, 그래서 나왔던 "정확도 99.9% / AUC 1.000"은
부풀려진 수치였다. 근접중복을 삭제하지 않고 **그룹으로 묶어 그룹 전체를 하나의
split에만** 배정하도록 바꾸고 나서야 신뢰할 수 있는 숫자(정확도 99.3% / 민감도
97.7%)가 나왔다.

여기까지가 종강 시점이고, 커밋 9개 · README 26KB · **자동 테스트 0개**였다.

---

## 2. 종강 이후 (2026-06-23 → 2026-08-26, 커밋 43개)

| | 종강본 | 현행 |
|---|---|---|
| 커밋 | 9 | **52** |
| 백엔드 | 1,151줄 / 7모듈 | **1,605줄 / 8모듈** (`safety.py` 신규) |
| 프론트 JS | 1,153줄 / 6모듈 | **3,167줄 / 9모듈 + `calibration.js`** |
| README | 26KB | **52KB** |
| 자동 테스트 | 없음 | **147개 + GitHub Actions CI** |
| 모델 | ResNet18 v3 (44MB) | **EfficientNet-B0 v5 (16MB)** |
| 판정 | 2단계 (정상/위험) | **3단계** (경계 25~50% 추가) |

굵직한 것만 시간순으로:

**7월 초 — 백본 교체와 TTA.** EfficientNet-B0 채택(test FN 7→5), 좌우반전 TTA 도입.
파일 크기는 44MB → 16MB로 오히려 줄었다. 단, TTA는 백본을 바꾸면 반드시 재측정해야
한다 — ResNet18 v4에서는 TTA가 FN을 7→10으로 **악화**시켰다.

**7월 중 — 실사진에서 드러난 문제.** held-out 실사진 검증에서 **밝은 홍채를 백내장으로
86% 오탐**하는 것을 발견. 밝은 홍채 정상 201장을 보강해 재학습(v5)한 결과
**오탐 86% → 18%, 특이도 98.3% → 99.5%.** 테스트셋 숫자가 좋아도 실사진에서 깨질 수
있다는 것을 이때 배웠다.

**7월 — 안전망.** pytest 도입(외부 의존성 전부 모킹, 10초 내), Python 3.11·3.13
매트릭스 CI, 학습↔서빙 일관성 테스트, Tailwind stale 검사.

**7~8월 — 앱이 '검사 도구'가 되다.** `calibration.js`로 화면 물리 크기를
캘리브레이션(신용카드 대조)하고, 그 위에서 **시력·대비감도 자가검사**를 구현했다.
시표의 실제 크기를 보존해야 의미가 있는 검사라 캘리브레이션이 전제 조건이었다.
문진도 증상 2문항 → **조건부 분기 13문항 + 응급 신호 감지**로 재설계.

**8월 — LLM 의료 오류 차단 (`safety.py`).** 외부 리뷰에서 Gemma가 실제로 이런 문장을
생성한 것이 확인됐다:

> "암슬러 격자가 정상이므로 녹내장과 관련된 심각한 황반부 문제 가능성이 낮다"

한 문장에 오류가 셋이다 — 암슬러(황반 검사)로 녹내장 결론을 냈고, 정상 결과로 질환을
'배제'했고, softmax 점수를 질병 확률로 해석했다. RAG는 '무엇을 참고할지'만 주입할 뿐
질환 간 인과를 지어내는 것은 막지 못했고, 프롬프트의 "확정 진단 금지" 지시도 이 유형은
막지 못했다. 그래서 **2단 방어**로 갔다: 1단은 애초에 LLM에게 해석을 시키지 않고
(결과 해석은 `app-findings.js`가 코드로 결정론적 생성, LLM은 생활 조언만), 2단은
새어 나온 위험 표현을 문장 단위로 버린다.

**8월 — 판독 보류 게이트.** 촬영 안내에서 **"플래시를 켜라"를 삭제**했다. 앱이 스스로
오탐을 유도하고 있었기 때문이다. 대신 플래시 반사 게이트와 흔들림 게이트를 두어
판독 불가한 사진은 결과 대신 재촬영 안내를 돌려준다. 발표장 오프라인 대비로 CDN
의존도 전부 제거(`static/vendor/`).

**진행 중 — v6.** v5가 **익상편을 88% 오분류**(150장 중 위험 120 + 경계 12)한다.
모델이 '수정체 혼탁'이 아니라 '눈이 하얗게 덮였는가'를 학습한 결과이고, 밝은 홍채
오탐과 같은 뿌리다. 익상편 225그룹을 `0_normal`에 넣어 그 경계를 다시 가르치는 중
(→ `docs_retrain_v6.md`).

---

## 3. 남는 교훈

세 가지가 반복해서 나타났다.

1. **좋아 보이는 숫자는 대체로 측정이 잘못된 것이다.** 99.9%는 데이터 누수였고,
   그걸 고친 99.3%조차 실사진에서는 밝은 홍채를 86% 오탐했다. 지금은 익상편이 같은
   자리에 있다. 매번 원인이 같다 — 모델이 우리가 가르치려던 것과 다른 것을 배웠다.
2. **앱이 스스로 문제를 만들고 있는 경우가 있다.** "플래시를 켜라"는 안내가 그랬다.
   모델을 고치기 전에 입력이 어떻게 만들어지는지를 먼저 봐야 했다.
3. **LLM에게는 해석을 맡기지 않는다.** 프롬프트로 막으려던 의료 오류는 구조를 바꾸고
   나서야(해석은 코드, LLM은 조언) 실제로 막혔다.

---

## 4. 스냅샷 폴더를 어떻게 할 것인가

넷 다 지워도 현행 저장소에는 영향이 없다. 다만 `eye_catch_Antigravity`와
`eye_catch_codex`는 종강본(`eye_catch_claude`)의 `compare/` 폴더에도 사본이 남아 있고,
6월 말 통합 작업의 근거이므로 발표·보고서를 쓸 때 참고할 여지가 있다. `eye_catch_old`는 동작하지
않는 미완성 스냅샷이라 보관 가치가 가장 낮다.

---

## 5. 스냅샷에서 되가져온 것 (2026-08-26)

다섯 폴더를 비교하다 **현행에 없는데 옛 버전에는 있던 것** 셋을 찾았다. 둘은
이식했고 하나는 기각했다.

### ✅ CORS 화이트리스트 (`eye_catch_codex`에서)

codex 버전에는 `EYECATCH_ALLOWED_ORIGINS=http://localhost:8000,...`이 있었는데,
현행은 `allow_origins=["*"]`로 모든 출처에 열려 있었다.

프론트가 같은 서버(`/static`)에서 서빙되므로 평소에는 same-origin이고, ngrok으로
공유해도 페이지와 API가 같은 도메인이라 CORS 헤더가 **아예 필요 없다.** 즉 쓰지도
않는 개방이었다. 기본값을 "미들웨어 미적용"으로 바꾸고, 프론트를 별도 도메인에 올릴
때만 `.env`의 `ALLOWED_ORIGINS`로 여는 방식으로 좁혔다.

- `app/core/config.py` — `allowed_origins` 설정 + `allowed_origins_list` 파서
- `app/main.py` — 목록이 비어 있으면 `CORSMiddleware`를 아예 달지 않음
- `.env.example` — `ALLOWED_ORIGINS` 항목과 설명 추가
- `tests/test_cors.py` — 파싱 4종 + 기본 상태에서 교차 출처에 허용 헤더가 없는지

### ✅ 의학 지식을 코드 밖으로 (`eye_catch_Antigravity`에서)

Antigravity 버전은 의학 지식을 `medical_guidelines.json`에 두었다. 현행은
`knowledge.py` 안에 파이썬 딕셔너리로 하드코딩돼 있어서, 의학 문장만 검수·수정하려는
사람도 코드를 건드려야 했다.

`data/medical_knowledge.json`으로 분리하고 `knowledge.py`는 로더 + 검증기로 바꿨다.
검색 로직(`retrieve_for_opinion` / `retrieve_for_chat` / `format_reference`)은 그대로다 —
이식 전후로 KB 내용과 세 함수의 반환값이 완전히 일치하는 것을 대조 확인했다.

로드 실패는 **감추지 않고 즉시 예외로 띄운다.** 참고지식 없이 LLM을 돌리면 곧바로
환각 위험이 커지므로, 조용히 빈 지식으로 굴러가는 쪽이 더 나쁘다. 검증 항목은
필수 항목 존재 · 빈 문장 · `keywords` 타입 · JSON 문법이다.

- `data/medical_knowledge.json` — 신규 (항목 5개)
- `app/services/knowledge.py` — 딕셔너리 → 검증하는 JSON 로더
- `tests/test_knowledge.py` — 로더 검증 7개 추가. 그중 하나는
  **`static/data.js`의 `disease:` 코드가 전부 KB에 있는지** 검사한다.
  프론트가 보내는 질환 코드와 KB 키가 어긋나면 소견서에 지식이 조용히 안 들어가는데,
  지금까지는 그걸 잡아줄 장치가 없었다.

### ❌ `remove_glare()` (`eye_catch_codex`) — 이식하지 않음

codex는 OpenCV inpaint로 플래시 반사를 **지우고** 분석했다. 현행은 반사가 눈동자를
덮으면 **판독을 보류하고 재촬영을 안내한다.**

지우는 쪽이 사용자 경험은 매끄럽지만, 반사에 덮인 영역은 정보가 사라진 것이지
복원 가능한 것이 아니다. inpaint가 채워 넣은 픽셀은 추정값이고, 그 위에서 나온
백내장 확률은 근거가 없다. 의료 스크리닝에서는 **모르는 것을 모른다고 말하는 쪽**이
맞아서 현행 게이트를 유지한다.

---

## 6. 팀 피드백 반영 (2026-09-02)

master(구 UI)에 잘못 체크아웃돼 있던 것을 `feature/unified-polish`로 되돌리고, master에서
하루 동안 고친 것 중 이 브랜치에 없던 것만 이식한 뒤 팀 요청을 반영했다.

### 이식 (master → unified-polish)
- 눈 좌/우를 MTCNN 랜드마크 순서가 아니라 사진 x좌표로 확정 (`eye_detector.py`, 테스트 5개)
- 문진 내역 라벨을 언어에 맞춤 — 영어 사용자 프롬프트에 '의사/환자'가 섞여 한국어로 답하던 경향
- 업로드 용량 초과 문구가 설정값을 따라가도록, 추가 질문 컨텍스트 5000자 상한, Leaflet 미로드 방어

### 팀 요청
- **리포트 AI 섹션을 3줄 요약으로** — 프롬프트 재작성(6개 언어 구조 동일), PDF는 줄마다 문단
- **촬영 팁**: '후면 카메라 권장' 삭제 → '흔들리지 않게'. **촬영 예시**(CC0 얼굴 사진: 좋은 예/흔들린 예)
- **글자 크기 A−/A+ 3단계** — 헤더는 px 고정, 3줄 요약 본문은 더 크게
- **카메라로 바로 찍기**(`capture`) — S펜 갤럭시에서 CSS `hover` 판별이 빗나가 JS 터치 판별로 교체
- **시야 체험을 질환 모달 밖으로** — 질환 탭 맨 아래 버튼 → 패널(4질환 칩), 장면은 SVG 삽화에서 CC0 실사진으로
- **폴더블·소형 화면**: 320px 헤더 축소, 768~1023px는 하단 탭 메뉴, 1024px 이상 메뉴 줄바꿈 금지

### 실기기(S25 Ultra) 버그
- 소견이 다 나온 뒤 "연결이 끊어졌습니다"로 바뀜 — 안드로이드 크롬의 `new Notification()` 예외가
  스트림 오류 처리로 흘러 완성된 소견을 덮어쓴 것. 알림·저장 동의를 소견 수신 밖으로 분리.

### 측정
- 흔들림 게이트: 1280px 얼굴 사진 기준 손떨림 12px(눈 폭 5.5%)부터 보류, 초점 흐림은 2px부터.
  모델 자체는 사진 폭 5% 손떨림까지 판정 변화가 정상 0/40·백내장 3/40.
- 테스트 147 → 162개.

### 같은 날 오후 — 안구 AI 점검과 외부 리뷰 2건 반영
- **v6 '민감도 하락'은 라벨 오류**: test에서 놓친 백내장 7~9장이 전부 정상 눈(백내장 클래스에 잘못 들어감). v5는
  학습 분할에서 외운 것. 10장을 `dataset_raw/label_errors_1_cataract/`로 옮기고 그룹맵에서 제거, 메타데이터에
  정정 지표(FN 0 / FP 2) 기록.
- **얼굴 경로에 눈/비-눈 게이트** (`scripts/build_eye_gate.py` → `app/models/eye_gate.npz`): 감은 눈·선글라스·안대가
  '정상 0.0%'로 나가던 구멍. 눈 클로즈업 경로의 중심 벡터 검증기(이마·볼·안저·풍경을 통과시킴)도 같은 게이트로 교체.
- TTA 기본 OFF(v6에서 손해), 반사 보류는 위험·경계일 때만, 게이트 순서 흔들림 → 눈 → 반사.
- **4단계 판정**과 범위 문구: '정상'/'백내장 판별' 대신 '뚜렷한 진행성 혼탁 특징 없음'/'혼탁 특징 분석'. 2~25점은
  '사진만으로 판단 어려움' + 눈 클로즈업 재촬영 권유(외부 테스트: 옅은 초기 혼탁 0~4.9점).
- 외부 리뷰(웹): 모달이 헤더에 가림(z-index), 지도 기본 위치를 '예시 위치'로 표시, 시력검사 실패 문구, 언어 전환 시
  체험 강도 유지, 재인코딩 메모 문구 축약, '진단 리포트' → '눈 건강 리포트', 데스크톱 시력 보정 2열, 업로드 오류 배너,
  리포트 % → x/100 점수, 업로드 전 개인정보 안내, 첫 화면에 사진 AI 범위 명시.
- 테스트 162 → 174개.

