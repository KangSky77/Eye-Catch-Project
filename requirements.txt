# Eye-Catch 의존성 (개발/운영용 — GPU 있는 환경)
# 설치:  uv pip install -r requirements.txt
#   (또는)  pip install -r requirements.txt
#
# torch는 CUDA 13.0 빌드를 PyTorch 공식 인덱스에서 받습니다.
# GPU가 없는 환경(CI 등)은 이 파일 대신 CPU wheel + requirements-base.txt를 쓰세요:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements-base.txt
--extra-index-url https://download.pytorch.org/whl/cu130

torch==2.12.0+cu130
torchvision==0.27.0+cu130

# 나머지 의존성은 전부 requirements-base.txt에 버전 고정으로 정리돼 있습니다.
-r requirements-base.txt

# ⚠️ MTCNN(facenet-pytorch)은 여기에 없습니다 — 반드시 --no-deps로 따로 설치할 것!
#    (구버전 torch를 강제로 끌고와서 설치된 torch 2.12+cu130을 다운그레이드하려고 함)
#    없어도 앱은 정상 동작합니다 (얼굴→눈 크롭 기능만 비활성화).
#      uv pip install facenet-pytorch --no-deps --python .venv
#    facenet이 import 시점에 요구하는 requests/tqdm은 requirements-base.txt에 포함돼 있습니다.
