# Eye-Catch 의존성 — torch/torchvision을 제외한 나머지 (전부 버전 고정)
#
# 이 파일이 따로 있는 이유:
#   requirements.txt는 torch를 CUDA 빌드(+cu130)로 고정하는데, 그 wheel은 수 GB라
#   GPU가 없는 환경(CI 러너 등)에서는 받을 이유도 방법도 없다. torch만 분리해두면
#   그런 환경이 CPU wheel을 따로 깔고 이 파일만 설치하면 된다.
#   → 사람이 개발용으로 설치할 때는 requirements.txt 하나만 보면 된다(이 파일을 포함함).
#
# 버전 고정 이유: 시드·분할·가중치 sha256까지 기록해 재현성을 관리하는 프로젝트인데
#   런타임 의존성만 열려 있으면, FastAPI/pydantic 메이저 업그레이드가 팀원 PC에서만
#   조용히 앱을 깨뜨린다. 아래는 실제로 검증된 조합(테스트 68건 통과 환경).
#   업그레이드할 때는 한 번에 하나씩 올리고 pytest로 확인할 것.

# --- 웹 서버 ---
fastapi==0.136.3
uvicorn[standard]==0.49.0
python-multipart==0.0.32   # FastAPI 파일 업로드(UploadFile)에 필요
pydantic-settings==2.14.1  # app/core/config.py

# --- 런타임 ---
httpx==0.28.1              # Ollama 스트리밍 호출
pillow==12.2.0             # 이미지 처리
numpy==2.4.4

# 선택: PostgreSQL 진단 저장 (없어도 앱은 동작)
asyncpg==0.31.0

# facenet-pytorch(MTCNN)가 import 시점에 요구 — facenet 자체는 --no-deps로 별도 설치
# (구버전 torch를 끌고와 설치된 torch를 다운그레이드하려 하기 때문. requirements.txt 참고)
requests==2.34.2
tqdm==4.68.2

# 데이터셋 도구용 (앱 런타임에는 불필요 — dedup_dataset.py에서만 사용)
ImageHash==4.3.2

# 테스트 (개발용 — tests/ 실행에만 필요)
pytest==9.1.1
