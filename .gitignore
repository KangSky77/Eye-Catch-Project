# 파이썬 캐시
__pycache__/
*.py[cod]

# 가상환경 (용량이 커서 올리면 안 됩니다)
.venv/
venv/

# 데이터셋 (수천 장의 사진을 올리면 깃허브가 거부합니다)
dataset/

# 대용량 모델 파일 (100MB 넘으면 에러 날 수 있음)
# 나중에 팀원에게는 따로 전달하거나 Git LFS를 써야 합니다.
*.pth

# 환경변수 (DB 비밀번호 등 비밀 정보 — 절대 깃에 올리면 안 됩니다)
.env

# 외부 실행파일 / 로그
ngrok.exe
train_log.txt
train_log_old.txt
train_log_v2.txt

# 임시 파일
_*.jpg
_*.png
_*.pdf